# 审计报告：2026-08-27 03:05 以来批次（上游执行 Agent 施工，主 Agent 五路审查）

> 2026-08-30 主 Agent 审查。五路并行审查（动效 / XML 链 / 映射表 / 统计与 Agent 工具 /
> converter+ZBS 两仓）+ 主 Agent 亲审书籍翻译烂尾。每条问题带 file:line 证据（审查期）。
> **结论速览：无数据损坏/安全级阻断洞；编译红已由主 Agent 修复（8f12db8）；
> 必修 5 项（下表 P1/P2），文档失真一批，挂账维持若干。**

## 一、书籍翻译（烂尾包）现状盘点——主 Agent 亲审

**实际状态比"烂尾"好**：一期 08-29 已实盘验收（段落枚举/注入契约、翻译服务、任务通道、
阅读器下拉），二期批次 1（译文视觉区分）✅、批次 3（词级对齐管线）✅、批次 4a/4b 落地；
4c 划词对照卡系用户裁定撤销（非缺陷）。**真正的问题是收梢没收住**：工作区 9 个 tsc 编译错
（TaskChannel 联合缺 `"book-translate"`、`book.language` 读错字段（全仓约定是
`primaryLanguage`）、两处条件数组 spread TS2556、`translationMeta` 死状态）——全部浅层
类型错误，主 Agent 已修复并提交（`8f12db8`，含任务面板「图书翻译」穷举标签补齐）。
契约测试 13/13 绿，cargo 54 绿。

**两个挂账阻断项维持用户裁定不动**（排查路径已在 docs/plans/book-translation-plan.md 写死）：
1. 译文模式翻页/章节跳转极卡（对照模式正常——DOM 量相同，差别仅在 display:none 原文段；
   嫌疑①foliate 分页对隐藏元素的重排路径 ②mousemove 高频路径 ③样式冲突；
   备选方案=transformer 不注入原文而非 CSS 隐藏，代价是切模式重载章节）。
2. hover 联动实测无效（冷启动时机已修仍无效；四个疑点与实证入口已记录：
   iframe 事件桥到达性 / ::highlight 规则入 iframe / data-block-index 属性实证 /
   spineIndex 坐标对位）。

**其他挂账**：4d 标注镜像（单独批次，侵入式增强，勿捎带）；book-translate 右下角任务栏
接入与图书侧冲突矩阵（plan「未来待办」节，随任务体系下一轮）。

## 二、必修清单（按优先级）

### P1 高危

| # | 问题 | 位置 | 修法 |
|---|---|---|---|
| 1 | **qwen3.7-max 快照行自相矛盾**：注释承诺「06-08 后快照是视觉型号→归一放行」，但基名行 `qwen3.7-max:false` 在剥离日期后拦截——视觉型号无声残废（本表裁定要根治的病） | vision-map.ts:171 vs :184 + canonicalSlug:356 | 基名行改 true 或删除基名行让日期比较生效（对照 qwen3.7-max-2026-06-08 语义） |

### P2 中危（真实用户可踩）

| # | 问题 | 位置 | 修法 |
|---|---|---|---|
| 2 | **Gemini budget 滑块失效**：UI 存数字字符串，google 分支只认 off/low/medium，其余一律 -1 动态预算——用户拖 4096 全部落空 | reasoning-map.ts:541（对照 dashscope :618-629 有 parseInt） | google 分支补 parseInt 数字档 |
| 3 | **auxThinkingLevel 把指令型 offParam 当 effort 泄漏**：deepseek/glm `thinking:disabled` 经 OpenRouter 辅助通道变 `{reasoning:{effort:"thinking:disabled"}}` 非法值 | reasoning-map.ts:727-731 + factory.ts:332-334 | 指令型 offParam 不映射 effort（识别 `:` 前缀） |
| 4 | **OpenRouter 聊天通道不分 transport**：switch 型 "on" → `effort:"on"`；budget 型数字串 → `effort:"4096"` | reasoning-map.ts:549-551 | 按 transport 分派（仅 effort 型直传） |
| 5 | **存量档位无迁移/钳制**：persist 默认 "medium" 在无 medium 档型号（deepseek low/high/max）上 UI 显示「思考·中」且直接下发 → 必 400 靠重试网兜底 | chat-settings-store.ts:27 + reasoning-map.ts:586-589 | UI 选择器对不在 levels 的存量值钳制到最近合法档 |

### P2 数据完整性

| # | 问题 | 位置 | 修法 |
|---|---|---|---|
| 6 | **AI 用量翻译主体漏计**：正文批次 generateBatch 与脚注批次 generateText 未取 usage 未记账——translate 是 aux 大头，面板严重低估；commit c10bb4f 声明与实现不符 | paper-translation-service.ts:495-501、:564-570、:576-582（对照 :252-253、:374-375 已记账点） | 三处 generateText 调用点补 recordAuxUsage |

### P2 合规语义（ZBS 仓）

| # | 问题 | 位置 | 修法 |
|---|---|---|---|
| 7 | **Sci-Hub 触达语义静默扩大**：双格式瀑布中 PDF 趟（含 scihub）在 XML 已命中后仍无条件执行——README.md:48,167 承诺「OA 命中不走它」是单格式口径 | zotero-brain-slim `paper_importer.py:903,918-919` | XML 命中后把 scihub 从 PDF 补档趟剔除（或改 README 口径——建议改代码，合规面不扩张） |

## 三、文档/口径失真一批（修起来都快）

- `docs/paper-format-contract.md` §一/§二未收 source.xml 与 references.json（source.xml 已是重解析链硬依赖）——XML 审查 P2-2。
- AI 工具面文案失真：processPaper 工具描述/报错 5 处仍「源 PDF」（tools/central/process-paper.ts:56,58,85,208,216）、tool-guard.ts:322 弹窗、paper-parse.ts:484 执行器报错——XML 审查 P2-1。
- `docs/archive/motion-system-plan.md:3-9` 状态行未追认批次 5（批次 3 验证数 21→46 陈旧）；§5.4 preload 措辞与代码 `preload="auto"` 不齐。
- 手册 `manual/01-overview.md:32`：第四卡已改「向量化用量」+ k/M/G 三档（5a12920/8726e66），手册停旧口径——巡检当天被自己批次的后续 commit 打破。
- readThread 提示词同步落空：描述加进了**无调用方的** `getToolDescriptions`（registry.ts:447-449），三处活提示词（central/paper/reader 硬编码清单）均未提；wiki/04-agent.md:8「工具清单由 getToolDescriptions() 生成」失实——修法：readThread 补进活清单，wiki 改口径。
- ZBS `README.md:12` 工具表漏 Springer 级（实为 9 级瀑布）；`.env.example` 缺 ELSEVIER/SPRINGER_NATURE/IEEE 三把 key（config.py:73-78 已加）。
- Papers_Converter README 全文未提 XML 管线。
- converter `_fetch_sn_media` 路径穿越隐患（XML href 直接落盘未剥 `../`，stage1_xml.py:417-424）——一行 `Path(filename).name` 堵上。

## 四、质量确认（无洞区，供放心）

- **动效批次 5 五连 commit**：keepalive 规格逐条吻合、行为变化透明申报；opacity 转正→用户否决→逐字节 revert（空 diff 实证无残留），教训沉淀进总册；壁纸冻结实现克制正确。
- **映射表枚举制重构**：最终形态 = 双表纯静态枚举 + 身份性归一（作者前缀/日期快照剥离），不对称默认方向两边都正确（vision 未知放行、reasoning 未知不下发）；UI 三形态联动正确；轻量任务思考控制 5 调用点全接入。
- **用量管线**：chat 采集搭车点正确（abort/error 不计数、多步聚合无重复）、kind 分账三端一致、迁移幂等；统计页 recharts 现成依赖、聚合全 O(n) 单遍、主题变量适配。
- **readThread**：召回逻辑与过滤正确性扎实（只留 text/quote、无工具细节泄漏、续读与流式免责周到）。
- **DOI 判重**：归一化与 Zotero 链一致、空 DOI 不拦；行为权衡（同 DOI 内容变了也拦）系用户拍板语义，手册需补「先删后导」。
- **converter XML 管线**：适配器结构干净、产物同构由构造保证、fixture 真跑（244 绿）、异常兜底完整。
- **ZBS**：无 key 入仓、Sci-Hub 默认关闭双闸、瀑布层级与 roadmap 一致、Elsevier 优雅跳过 12 场景测试。

## 五、待办状态刷新（用户口径对齐）

- **站点线**：公安备案号已下 → 可推进：EdgeOne 绑 bettersageread.cn、页脚填备案号、
  KV 批后配下载计数器（`site/DEPLOY.md`）。site/ 仓无任何未提交改动。
- **Elsevier 挂 VPN 实测**（ZBS roadmap Phase 1.5）：人工待办，key 已实测元数据 200。
- **发版 v0.3.0**：按用户最新口径 = 动效全批（含批次 5，已落）+ ZBS 适配（XML 管线已落）
  + **本报告必修清单清零** → 然后走总册任务卡 8 流程。
- **想法池新增**：MessageItem 级 memo（chat-messages.tsx 中型重构）；统计页紧凑单位
  1000k 越档与饼图跨 provider 同名（cosmetic）；ai_usage.rs 共享池与 LIMIT（长期）。
