# 阅读器修复施工单（A–E + 附带项）

> 2026-09-15 用户批准版。状态随施工推进更新。
> 背景：标题/公式/表格/TOC 四连之后的第二轮。第一轮 5 个 commit 见 git log（h5h6、HMR 守卫、TOC 打瞌睡/误判章末、公式导轨、表格防污染）。

## A. 章节定位偶发掉到章末（滚动中 5.x→5.9 鬼影）——【已修，待用户复验】

- **探针实锤**（用户两轮真实滚动，132+103 条 relocate 记录）：掉 5.9 的 relocate 可见 Range 起点与视口吻合、**末尾恒在 body 末尾**——下方未加载完的内容（图片 0 高塌陷等）被压缩进当前视窗，TreeWalker 把章末节点误判为"最后可见节点"；旧语义"最后一个不晚于 Range 末尾的锚点"使全部锚点判"已过" → 兜底返回本章末项。布局加载完成后同位置恢复正常，形成交替鬼影。
- **修复**（`5f8669c`，progress.js）：判定锚点改为与"折叠到 Range 起点"比较（视口顶部语义），对布局塌陷免疫，与 CFI/进度保存语义对齐。
- **版本确认**：实例行为测试证明运行中新代码（构造末端越界的 Range，旧代码必答章末，实测答正确小节）。
- **残留风险**：自有脚本始终未复现用户侧的触发条件（真实滚轮+特定布局时序），故保留常驻探针（见下）随时取证。

## D. 超宽/超高表格分页崩溃 ——【已修】

- 实测英文版 Willingness Test 表：**宽度不超栏（scrollWidth 检测不到）但尾行被挤压成 700px 高单元格**，分片+表头重复叠字。
- 修复（本轮 commit）：paginator `markOverflowTables` 双路触发（a. scrollWidth>clientWidth；b. 任一行 > 0.8×栏高）→ `.sr-table-scroll` 滚动框：表格恢复自然宽度（行高回落），框锁栏宽（不压邻栏）、高度阈值上限（分页=栏高/滚动=视口高），横纵双导轨（hover 显形、滚轮滚动链）。FG 中文版百分比表格零包装实测；顺带修 foliate 无 body 文档崩溃。

## E. 小修包 ——【待施工】

1. **E1 工具调用折叠失效**：`chat-messages.tsx:625` T6 折叠组疑似被推理 part 打断邻接。恢复折叠并对推理 part 健壮。
2. **E2 工具卡片恒显 0 results**：readBookSection 成功也显示 0——计数口径不认对象型结果。
3. **E3 describeTool 死循环**：目录牌模式作用域注册不齐，错误提示引导 Agent 调用未注册的 describeTool。对齐注册或清理指引。
4. **E4 rag* 报错误导**：未向量化书的报错统一为"本书未建立向量索引"；评估此类书 rag* 工具是否直接不注册。
5. **E6 对话标题自动命名**：首轮问答后自动命名一次 + 新建对话时存量补命名；手动命名/星标不动。
6. **E7 历史对话当前项高亮**：主题色高亮 + hover 动效，遵守"动效模式"设置。

## C+B. Agent 内容保真 ——【待施工，合并进行】

- **C**：`readBookSection` JS 读取层重写（foliate/epub.js 解析）：深层小节直读 + `startOffset` 续读 + 结构化提取；Rust 旧命令保留兜底。并入 E5（标题匹配语义改"精确>前缀>模糊"，要 5 章给 5.9 的病）。
- **B**：公式 MathML→LaTeX（新依赖 `mathml-to-latex`，用户已批准），失败回退原样 MathML，绝不扁平化；选区引用与直读两路径；表格转 Markdown 表格。论文侧引用公式同样经源映射回解（paper-highlight-locator 已有 quote→源机制）。
- **RAG 扁平**：向量化前 `<math>`→占位符治标；治本=转换器写 `<annotation encoding="application/x-tex">`（已挂账 next-round-backlog.md，用户明确要求勿忘）。

## 常驻探针（A 的取证保障）

relocate 环形日志（reason/label/range 起末 XPath+offset/视窗/bodyH/事件速率）常驻实例。用户再目击 5.9 时导出即可锁定机制，无需再复现。

## 施工顺序

A✅ → D✅ → E1/E2/E3/E4 → C+B → E6/E7
