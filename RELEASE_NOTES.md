# Better SageRead v0.2.3 — 更新不再强推 + 解析可靠性三连修

修补迭代：更新链路两个事故的根修、解析任务可靠性三连修、引文排版与模型支持表修订。
**建议所有用户更新——本次起，更新永远先问过你。**

## 📦 下载（Windows x64）

- **推荐**：`Better.SageRead_0.2.3_x64-setup.exe`（NSIS 安装包）
- 备选：`Better.SageRead_0.2.3_x64_en-US.msi`
- 已装旧版的用户可直接在应用内「设置 → 检查更新」升级

> ⚠️ 本版本仍无 macOS 版（未签名 + 转换器无 mac 二进制），补齐后恢复。

## 🔧 修复

- **更新不再强推**：此前启动时发现新版本会直接下载安装（来不及拒绝）。现在启动不做任何自动检查；只在「设置 → 检查更新」手动检查时弹确认框（版本号 + 完整更新说明，主题化排版），「立即更新 / 以后再说」由你决定。
  ⚠️ 注意：从 v0.2.2 更新到本版仍会不经确认自动装最后一次——确认门是本版才装上的，之后版本都走它
- **快捷方式双开收敛**：安装器模式钉死系统级，更新不再在桌面写出第二份同名图标（已有的旧图标手动删一次即可）
- **取消解析立即生效**：此前取消转换后进程还拖 30-60 秒才退出（子进程没被杀到），现在整树即时终止
- **刷新页面不再丢解析任务**：此前刷新后解析中的论文进度卡消失、产物滞留不入库；现在刷新后自动恢复监控与落库链路，已完成的产物会补入库
- **检索维度降级**：更换嵌入模型后、重新向量化前，语义检索自动降级为关键词检索并提示，不再报错

## ✨ 功能与体验

- **引文排版恢复印刷上标**：文内引文链接显示为右上角小字（贴近论文原貌），可点击跳转不变
- **RSC 式上标引文追回**（重新解析后生效）：被误吞进公式的引文标记（渲染成"平方"那种）现在能正确还原为引文并可跳转
- **多模态模型支持表修订**：Kimi k3/k2.6 全系、Qwen 3.5+ 主线、GLM-5v 等视觉模型不再被误拦，可以正常带图对话；修正 o3-mini 等误放行；思考强度映射按各厂最新文档修订（DeepSeek low 档、Gemini 3 系分档等）

## 🙏 致谢

Better SageRead 基于 [xincmm/sageread](https://github.com/xincmm/sageread)
独立演进而来，感谢原作者奠定基础。详见仓库
[NOTICE](https://github.com/Feplus2/better-sageread/blob/main/NOTICE)。
