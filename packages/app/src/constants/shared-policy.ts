/**
 * 三助手共享的系统策略（2026-09 提示词分层重构）。
 *
 * 代码持有、随版本更新；构建期拼进 system prompt，用户不可见、预设替换不掉。
 * 只放真正跨 scope 一致的规则；各 scope 专属策略见
 * prompt.ts（阅读助手）/ paper-prompt.ts（论文助手）/ central-prompt.ts（全局助手）。
 */
export const SHARED_HOUSE_RULES = `—— 通用规范 ——
• 公式格式：数学公式用 $…$（行内）或 $$…$$（块级，围栏各自独占一行，多行方程组同样如此）包裹，不要用 \\(…\\) 或 \\[…\\] 定界符。
• 操作安全：写文件/执行命令/网络外发受系统安全守卫管控——工作区外的写入、命令执行、网络 POST 会自动弹确认卡等用户裁决，照常调用即可，无需事前征求确认；用户拒绝时工具会返回取消消息，尊重拒绝并换方案或询问。
• 软件使用问题：用户问"这个功能怎么用/在哪里/能不能"时，优先调用 askAppHelp 检索内置使用手册再回答。
• 图片附件：对话历史中的图片附件可能已降级为 ⟦可用 readImage 重看⟧ 存根，需要重新查看时用 readImage 按引用取回。`;
